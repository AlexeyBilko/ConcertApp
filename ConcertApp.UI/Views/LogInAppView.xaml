<UserControl x:Class="ConcertApp.UI.Views.LogInAppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ConcertApp.UI.Views"
            xmlns:vm="clr-namespace:MyBank.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="385" d:DesignWidth="710"
             DataContext="{Binding Source={StaticResource locator}, Path=LogInViewModel}">
    <Grid Background="#EFEFEF">
        <Grid.RowDefinitions>
            <RowDefinition Height="235"></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Label FontFamily="Berlin Sans FB Demi" FontSize="19"  Grid.Row="1" Margin="174,29.6,-30.6,20.2" Grid.RowSpan="2">Email</Label>
            <Label FontFamily="Berlin Sans FB Demi" FontSize="19"  Grid.Row="2" Margin="174,23.2,-30.6,-12.8">Password</Label>

            <TextBox Text="{Binding TelNumber}" FontSize="14" Grid.Row= "1" Width="250" Height="42" Margin="171,15,72.2,9.6" Style="{DynamicResource TextBoxCornerRadius7}" Grid.ColumnSpan="2">
                <TextBox.ToolTip>
                    <ToolTip>
                        Enter your phone number
                        (Example: 380951111111)
                    </ToolTip>
                </TextBox.ToolTip>
            </TextBox>
            <TextBox Text="{Binding Password}" FontSize="14" Grid.Row= "2" Width="250" Height="42"  Margin="171,10.4,72.2,15" Style="{DynamicResource TextBoxCornerRadius7}" Grid.ColumnSpan="2">
                <TextBox.ToolTip>
                    <ToolTip>
                        Enter your password
                    </ToolTip>
                </TextBox.ToolTip>
            </TextBox>
        </Grid>
        <Grid Grid.Row="1">
            <Button Command="{Binding LogInCommand}" Background="#FF2E00" Grid.Row="1" Margin="30" Width="200" Height="40" Style="{DynamicResource ButtonStyle1}">
                <Label Foreground="#efefef" FontFamily="Berlin Sans FB Demi" FontSize="22">Log in</Label>
                <Button.ToolTip>
                    <ToolTip>
                        Log I
                    </ToolTip>
                </Button.ToolTip>
            </Button>
        </Grid>
    </Grid>
</UserControl>
