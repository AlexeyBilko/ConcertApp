<UserControl x:Class="ConcertApp.UI.Views.ListConcertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ConcertApp.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Source={StaticResource locator}, Path=ListConcertsViewModel}">

    <UserControl.Resources>
        <Style TargetType="{x:Type ListViewItem}" x:Key="ListViewItemStyle">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter Property="IsSelected" Value="True" />
                    <Setter Property="BorderThickness" Value="2"></Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.36*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ListView 
                 ItemsSource="{Binding Concerts}" Grid.Row="1"
                 SelectedItem="{Binding SelectedConcert}"
                 Grid.Column="1"
                 BorderBrush="White"
                 ItemContainerStyle="{StaticResource ListViewItemStyle}"
                 >
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel MaxWidth="700" Margin="30" MinWidth="160">

                    </WrapPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Red" BorderThickness="6" CornerRadius="2">
                        <Grid>
                            <StackPanel Margin="10" Grid.Row="0" MaxWidth="180">


                                <Image Source="{Binding Photo}" 
                                    Width="240"
                                    Height="250"></Image>
                                <Label Height="10"></Label>
                                <TextBlock Text="{Binding Title}" FontWeight="Black" TextWrapping="WrapWithOverflow" 
                                           HorizontalAlignment="Center"
                                           FontSize="20"></TextBlock>
                                <Label Height="10"></Label>
                                <Button Style="{DynamicResource ButtonStyle1}"  Background="OrangeRed" Foreground="White" FontWeight="Bold" FontSize="15">
                                    Order
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>

        <StackPanel Orientation="Vertical">
            <Border BorderThickness="7" BorderBrush="Red" CornerRadius="30" Margin="5,40,0,0">
                <Calendar BorderThickness="0" Margin="10" Foreground="Red" Background="White" MaxWidth="200">


                </Calendar>
            </Border>

            <Button FontSize="20" 
                    HorizontalAlignment="Center" FontWeight="DemiBold" Margin="0,20,0,0" 
                    Foreground="Red" Background="White" BorderBrush="White" 
                    Command="{Binding GetConcertsCommand}"
                    >Concert</Button>
            
            <Button 
                FontSize="20" HorizontalAlignment="Center" 
                FontWeight="DemiBold" Margin="0,4,0,0" Foreground="Red" 
                Background="White" BorderBrush="White"
                Command="{Binding GetTheatreCommand}">Theatre</Button>
            
            
            <Button FontSize="20" HorizontalAlignment="Center" 
                    FontWeight="DemiBold" Margin="0,4,0,0" 
                    Foreground="Red" Background="White" 
                    BorderBrush="White"
                    Command="{Binding GetFestivalsCommand}">Festival</Button>
            
            <Button FontSize="20" HorizontalAlignment="Center" 
                    FontWeight="DemiBold" Margin="0,4,0,0" 
                    Foreground="Red" Background="White" 
                    BorderBrush="White"
                    Command="{Binding GetHumorCommand}">Humor</Button>
            <Image
                Source="line.png" Width="200" Height="50" HorizontalAlignment="Left"
                Margin="0,20,0,0">
                
            </Image>
            
        </StackPanel>
    </Grid>
</UserControl>
